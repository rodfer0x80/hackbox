version: "2.1"
services:
  hackbox:
    image: hackbox
    container_name: hackbox
    user: 1000:1000
    volumes:
      - ./opt:/root/run
    ports:
      - 1337:1337
    restart: unless-stopped
    entrypoint: ./run.sh
